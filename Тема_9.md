# Тема 9. Концепции и принципы ООП 
Отчет по Теме #9 выполнил(а):
- Усова Софья Сергеевна
- ИВТ-23-2

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |

## Лабораторная работа №1
### Допустим, что вы решили оригинально и немного странно познакомится с человеком. Для этого у вас должен быть написан свой класс на Python, который будет проверять угадал ваше имя человек или нет. Для этого создайте класс, указав в свойствах только имя. Дальше создайте функцию  init (), а в ней сделайте проверку на то угадал человек ваше имя или нет. Также можете проверить что будет, если в этой функции указав атрибут, который не указан в вашем классе, например, попробуйте вызвать фамилию.

```python
class Ivan:
    __slots__=['name']

    def __init__(self, name):
        if name == 'Иван':
            self.name=f"Да, я {name}"
        else:
            self.name = f"Я не {name}, а Иван"

person1= Ivan('Алексей')
person2= Ivan('Иван')
print(person1.name)
print(person2.name)

person2.surname ="Петров"
```
### Результат.
![Скриншот задания](pic_9/лаб9_1.jpg)

## Выводы

Создание класса с использованием slots для ограничения списка доступных атрибутов, что экономит память и предотвращает добавление случайных новых свойств; метод init выполняет проверку имени и задает соответствующее сообщение, попытка создания атрибута вне slots вызывает ошибку. 

## Лабораторная работа №2
### Вам дали важное задание, написать продавцу мороженого программу, которая будет писать добавили ли топпинг в мороженое и цену после возможного изменения. Для этого вам нужно написать класс, в котором будет определяться изменили ли состав мороженого или нет. В этом классе  реализуйте  метод,  выводящий  на  печать  «Мороженое  с {ТОППИНГ}» в случае наличия добавки, а иначе отобразится следующая фраза: «Обычное мороженое». При этом программа должна воспринимать как топпинг только атрибуты типа string.

```python
class Icecream:
    def __init__(self, ingredient=None):
        if isinstance(ingredient, str):
            self.ingredient = ingredient
        else:
            self.ingredient = None

    def composition(self):
        if self.ingredient:
            print(f"Мороженое с {self.ingredient}")
        else:
            print("Обычное мороженое")


icecream = Icecream()
icecream.composition()
icecream = Icecream("шоколадом")
icecream.composition()
icecream = Icecream(5)
icecream.composition()
```
### Результат.
![Скриншот задания](pic_9/лаб9_3.jpg)
## Выводы

Класс с атрибутом ingredient, который инициализируется только в случае строкового значения, метод composition выводит информацию о наличии или отсутствии топпинга; применяется проверка типа через isinstance. 

## Лабораторная работа №3
### Петя – начинающий программист и на занятиях ему сказали реализовать икапсу…что-то. А вы хороший друг Пети и ко всему прочему прекрасно знаете, что икапсу…что-то – это инкапсуляция, поэтому решаете помочь вашему другу с написанием класса с инкапсуляцией. Ваш класс будет не просто инкапсуляцией, а классом с сеттером, геттером и деструктором. После написания класса вам необходимо продемонстрировать что все написанные вами функции работают. Также вас необходимо объяснить Пете почему на скриншоте ниже в консоли выводится ошибка.

```python
class MyClass:
    def __init__(self, value):
        self._value = value

    def  set_value(self, value):
        self._value = value

    def  get_value(self):
        return self._value

    def  del_value(self):
        del self._value


obj = MyClass(42)
print (obj.get_value())
obj.set_value(45)
print(obj.get_value())
obj.set_value(100)
print(obj.get_value())
obj.del_value()
print(obj.get_value())
```
### Результат.
![Скриншот задания](pic_9/лаб9_2.jpg)
## Выводы

Демонстрация инкапсуляции с приватным атрибутом _value, сеттером set_value, геттером get_value и деструктором del_value, после удаления атрибута попытка его получить вызывает ошибку

  
## Лабораторная работа №4
### Вам прекрасно известно, что кошки и собаки являются млекопитающими, но компьютер этого не понимает, поэтому вам нужно написать три класса: Кошки, Собаки, Млекопитающие. И при помощи “наследования” объяснить компьютеру что кошки и собаки – это млекопитающие. Также добавьте какой-нибудь свой атрибут для кошек и собак, чтобы показать, что они чем-то отличаются друг от друга.

```python
class Mammal:
    className = 'Mammal'

class Dog(Mammal):
    species = 'canine'
    sound = 'wow'

class Cat(Mammal):
    species = 'feline'
    sound = 'meow'

dog = Dog()
print(f"Dog is {dog.className}, but they say {dog.sound}")
cat = Cat()
print(f"Cat is {cat.className}, but they say {cat.sound}")
```
### Результат.
![Скриншот задания](pic_9/лаб9_4.jpg)
## Выводы

Иерархия классов с наследованием: общий класс Mammal и подклассы Dog и Cat с уникальными атрибутами species и sound, показаны механизмы наследования и использования атрибутов классов. 

## Лабораторная работа №5
### На разных языках здороваются по-разному, но суть остается одинаковой, люди друг с другом здороваются. Давайте вместе с вами реализуем программу с полиморфизмом, которая будет описывать всю суть первого предложения задачи. Для этого мы можем выбрать два языка, например, русский и английский и написать для них отдельные классы, в которых будет в виде атрибута слово, которым здороваются на этих языках. А также напишем функцию, которая будет выводить информацию о том, как на этих языках здороваются. Заметьте, что для решения поставленной задачи мы использовали декоратор @staticmethod, поскольку нам не нужны обязательные параметры-ссылки вроде self.

```python
class Russian:
    @staticmethod
    def greeting():
        print("Привет")

class English:
    @staticmethod
    def greeting():
        print("Hello")

def greet(language):
    language.greeting()

ivan = Russian()
greet(ivan)
ivan = English()
greet(ivan)
```
### Результат.
![Скриншот задания](pic_9/лаб9_5.jpg)
## Выводы

Демонстрация полиморфизма с помощью статических методов greeting в классах Russian и English, функция greet принимает объект любого из этих классов и вызывает соответствующий метод для вывода приветствия, что показывает динамическое связывание методов в Python.

## Самостоятельная работа №1
### Вызовите справку по садоводству

```python
class Tomato:
    # Статическое свойство states содержит все стадии созревания помидора
    states = {0: 'отсутствует', 1: 'цветение', 2: 'зеленый', 3: 'красный'}

    def __init__(self, index):
        # Динамические свойства:
        # _index — индекс помидора (уникальный для каждого объекта)
        # _state — текущая стадия созревания (начинается со стадии отсутствует)
        self._index = index
        self._state = 0

    def grow(self):
        # Переводит томат на следующую стадию созревания, пока не достигнут максимальный этап
        if self._state < max(Tomato.states.keys()):
            self._state += 1

    def is_ripe(self):
        # Проверяет, созрел ли томат (стадия 'красный')
        return self._state == max(Tomato.states.keys())

    def get_state(self):
        # Возвращает текущую стадию созревания текстом
        return Tomato.states[self._state]


class TomatoBush:
    def __init__(self, count):
        # Динамическое свойство tomatoes — список из count помидоров (объектов Tomato)
        self.tomatoes = [Tomato(index) for index in range(1, count + 1)]

    def grow_all(self):
        # Переводит все помидоры в списке на следующую стадию созревания
        for tomato in self.tomatoes:
            tomato.grow()

    def all_are_ripe(self):
        # Проверяет, что все помидоры созрели
        return all(tomato.is_ripe() for tomato in self.tomatoes)

    def give_away_all(self):
        # Очищает список помидоров после сбора урожая
        self.tomatoes.clear()


class Gardener:
    def __init__(self, name, plant):
        # Динамические свойства:
        # name — публичное свойство с именем садовника
        # _plant — приватное свойство с растением (объект класса TomatoBush)
        self.name = name
        self._plant = plant

    def work(self):
        # Садовник ухаживает за растением, вызывая его рост
        print(f'Садовник {self.name} ухаживает за растением...')
        self._plant.grow_all()

    def harvest(self):
        # Пробует собрать урожай, если все помидоры созрели
        if self._plant.all_are_ripe():
            print('Все помидоры созрели! Садовник собирает урожай.')
            self._plant.give_away_all()
        else:
            print('Помидоры еще не созрели. Нужно продолжать ухаживать.')

    @staticmethod
    def knowledge_base():
        # Выводит справку по садоводству
        print('''
        Справка по садоводству:
        1. Садовник ухаживает за растением, заставляя его расти.
        2. Томаты проходят стадии созревания: отсутствует, цветение, зеленый, красный.
        3. Растение можно собирать, только когда все томаты созрели.
        ''')

# Тестирование работы программы

# Вызов справки по садоводству
Gardener.knowledge_base()
```
### Результат.
![Скриншот задания](pic_9/сам9_1.jpg)
## Выводы

Реализован класс Tomato со статическим свойством states, отражающим стадии созревания томата, где в методе init инициализируется индекс и стартовая стадия; методы grow переводят томат на следующую стадию, is_ripe проверяет спелость, get_state возвращает текстовое описание стадии. Класс TomatoBush содержит список томатов и управляет массовым созреванием через grow_all и проверкой созревания всех томатов all_are_ripe; метод give_away_all предназначен для очистки урожая. Класс Gardener содержит имя садовника и ссылку на растение, методы work ухаживают за растением, вызывая grow_all, harvest собирает урожай, если все томаты спелые, иначе выводит предупреждение; staticmethod knowledge_base выводит справку по садоводству.
  
## Самостоятельная работа №2
### Создайте объекты классов TomatoBush и Gardener

```python
class Tomato:
    # Статическое свойство states содержит все стадии созревания помидора
    states = {0: 'отсутствует', 1: 'цветение', 2: 'зеленый', 3: 'красный'}

    def __init__(self, index):
        # Динамические свойства:
        # _index — индекс помидора (уникальный для каждого объекта)
        # _state — текущая стадия созревания (начинается со стадии отсутствует)
        self._index = index
        self._state = 0

    def grow(self):
        # Переводит томат на следующую стадию созревания, пока не достигнут максимальный этап
        if self._state < max(Tomato.states.keys()):
            self._state += 1

    def is_ripe(self):
        # Проверяет, созрел ли томат (стадия 'красный')
        return self._state == max(Tomato.states.keys())

    def get_state(self):
        # Возвращает текущую стадию созревания текстом
        return Tomato.states[self._state]


class TomatoBush:
    def __init__(self, count):
        # Динамическое свойство tomatoes — список из count помидоров (объектов Tomato)
        self.tomatoes = [Tomato(index) for index in range(1, count + 1)]

    def grow_all(self):
        # Переводит все помидоры в списке на следующую стадию созревания
        for tomato in self.tomatoes:
            tomato.grow()

    def all_are_ripe(self):
        # Проверяет, что все помидоры созрели
        return all(tomato.is_ripe() for tomato in self.tomatoes)

    def give_away_all(self):
        # Очищает список помидоров после сбора урожая
        self.tomatoes.clear()


class Gardener:
    def __init__(self, name, plant):
        # Динамические свойства:
        # name — публичное свойство с именем садовника
        # _plant — приватное свойство с растением (объект класса TomatoBush)
        self.name = name
        self._plant = plant

    def work(self):
        # Садовник ухаживает за растением, вызывая его рост
        print(f'Садовник {self.name} ухаживает за растением...')
        self._plant.grow_all()

    def harvest(self):
        # Пробует собрать урожай, если все помидоры созрели
        if self._plant.all_are_ripe():
            print('Все помидоры созрели! Садовник собирает урожай.')
            self._plant.give_away_all()
        else:
            print('Помидоры еще не созрели. Нужно продолжать ухаживать.')

    @staticmethod
    def knowledge_base():
        # Выводит справку по садоводству
        print('''
        Справка по садоводству:
        1. Садовник ухаживает за растением, заставляя его расти.
        2. Томаты проходят стадии созревания: отсутствует, цветение, зеленый, красный.
        3. Растение можно собирать, только когда все томаты созрели.
        ''')


# Тестирование работы программы

# Создание куста с 3 томатами и садовника с именем Иван
bush = TomatoBush(3)
gardener = Gardener('Иван', bush)



```
### Результат.
![Скриншот задания](pic_9/сам9_2.jpg)
## Выводы

Дополнительно создаются объекты TomatoBush и Gardener — демонстрация создания объектов и привязки садовника к растению. 
  
## Самостоятельная работа №3
### Используя объект класса Gardener, поухаживайте за кустом с помидорами

```python
class Tomato:
    # Статическое свойство states содержит все стадии созревания помидора
    states = {0: 'отсутствует', 1: 'цветение', 2: 'зеленый', 3: 'красный'}

    def __init__(self, index):
        # Динамические свойства:
        # _index — индекс помидора (уникальный для каждого объекта)
        # _state — текущая стадия созревания (начинается со стадии отсутствует)
        self._index = index
        self._state = 0

    def grow(self):
        # Переводит томат на следующую стадию созревания, пока не достигнут максимальный этап
        if self._state < max(Tomato.states.keys()):
            self._state += 1

    def is_ripe(self):
        # Проверяет, созрел ли томат (стадия 'красный')
        return self._state == max(Tomato.states.keys())

    def get_state(self):
        # Возвращает текущую стадию созревания текстом
        return Tomato.states[self._state]


class TomatoBush:
    def __init__(self, count):
        # Динамическое свойство tomatoes — список из count помидоров (объектов Tomato)
        self.tomatoes = [Tomato(index) for index in range(1, count + 1)]

    def grow_all(self):
        # Переводит все помидоры в списке на следующую стадию созревания
        for tomato in self.tomatoes:
            tomato.grow()

    def all_are_ripe(self):
        # Проверяет, что все помидоры созрели
        return all(tomato.is_ripe() for tomato in self.tomatoes)

    def give_away_all(self):
        # Очищает список помидоров после сбора урожая
        self.tomatoes.clear()


class Gardener:
    def __init__(self, name, plant):
        # Динамические свойства:
        # name — публичное свойство с именем садовника
        # _plant — приватное свойство с растением (объект класса TomatoBush)
        self.name = name
        self._plant = plant

    def work(self):
        # Садовник ухаживает за растением, вызывая его рост
        print(f'Садовник {self.name} ухаживает за растением...')
        self._plant.grow_all()

    def harvest(self):
        # Пробует собрать урожай, если все помидоры созрели
        if self._plant.all_are_ripe():
            print('Все помидоры созрели! Садовник собирает урожай.')
            self._plant.give_away_all()
        else:
            print('Помидоры еще не созрели. Нужно продолжать ухаживать.')

    @staticmethod
    def knowledge_base():
        # Выводит справку по садоводству
        print('''
        Справка по садоводству:
        1. Садовник ухаживает за растением, заставляя его расти.
        2. Томаты проходят стадии созревания: отсутствует, цветение, зеленый, красный.
        3. Растение можно собирать, только когда все томаты созрели.
        ''')


# Тестирование работы программы


# Создание куста с 3 томатами и садовника с именем Иван
bush = TomatoBush(3)
gardener = Gardener('Иван', bush)

# Садовник ухаживает за растением
gardener.work()


```
### Результат.
![Скриншот задания](pic_9/сам9_3.jpg)
## Выводы

С помощью объекта Gardener показано, как ухаживать за кустом, что вызывает созревание томатов поэтапно.

## Самостоятельная работа №4
### Попробуйте собрать урожай, когда томаты еще не дозрели. Продолжайте ухаживать за ними

```python
class Tomato:
    # Статическое свойство states содержит все стадии созревания помидора
    states = {0: 'отсутствует', 1: 'цветение', 2: 'зеленый', 3: 'красный'}

    def __init__(self, index):
        # Динамические свойства:
        # _index — индекс помидора (уникальный для каждого объекта)
        # _state — текущая стадия созревания (начинается со стадии отсутствует)
        self._index = index
        self._state = 0

    def grow(self):
        # Переводит томат на следующую стадию созревания, пока не достигнут максимальный этап
        if self._state < max(Tomato.states.keys()):
            self._state += 1

    def is_ripe(self):
        # Проверяет, созрел ли томат (стадия 'красный')
        return self._state == max(Tomato.states.keys())

    def get_state(self):
        # Возвращает текущую стадию созревания текстом
        return Tomato.states[self._state]


class TomatoBush:
    def __init__(self, count):
        # Динамическое свойство tomatoes — список из count помидоров (объектов Tomato)
        self.tomatoes = [Tomato(index) for index in range(1, count + 1)]

    def grow_all(self):
        # Переводит все помидоры в списке на следующую стадию созревания
        for tomato in self.tomatoes:
            tomato.grow()

    def all_are_ripe(self):
        # Проверяет, что все помидоры созрели
        return all(tomato.is_ripe() for tomato in self.tomatoes)

    def give_away_all(self):
        # Очищает список помидоров после сбора урожая
        self.tomatoes.clear()


class Gardener:
    def __init__(self, name, plant):
        # Динамические свойства:
        # name — публичное свойство с именем садовника
        # _plant — приватное свойство с растением (объект класса TomatoBush)
        self.name = name
        self._plant = plant

    def work(self):
        # Садовник ухаживает за растением, вызывая его рост
        print(f'Садовник {self.name} ухаживает за растением...')
        self._plant.grow_all()

    def harvest(self):
        # Пробует собрать урожай, если все помидоры созрели
        if self._plant.all_are_ripe():
            print('Все помидоры созрели! Садовник собирает урожай.')
            self._plant.give_away_all()
        else:
            print('Помидоры еще не созрели. Нужно продолжать ухаживать.')

    @staticmethod
    def knowledge_base():
        # Выводит справку по садоводству
        print('''
        Справка по садоводству:
        1. Садовник ухаживает за растением, заставляя его расти.
        2. Томаты проходят стадии созревания: отсутствует, цветение, зеленый, красный.
        3. Растение можно собирать, только когда все томаты созрели.
        ''')


# Тестирование работы программы

# Создание куста с 3 томатами и садовника с именем Иван
bush = TomatoBush(3)
gardener = Gardener('Иван', bush)

# Садовник ухаживает за растением
gardener.work()

# Попытка собрать урожай, когда томаты еще не созрели
gardener.harvest()

# Продолжение ухода за растением
gardener.work()
gardener.work()
```
### Результат.
![Скриншот задания](pic_9/сам9_4.jpg)
## Выводы

Пытаются собрать урожай, когда помидоры еще не созрели; садовник продолжает за ними ухаживать, вызывает grow_all у томатов, демонстрируя условие невозможности сбора «неспелого» урожая и необходимость продолжения заботы.
  
## Самостоятельная работа №5
### Соберите урожай

```python
class Tomato:
    # Статическое свойство states содержит все стадии созревания помидора
    states = {0: 'отсутствует', 1: 'цветение', 2: 'зеленый', 3: 'красный'}

    def __init__(self, index):
        # Динамические свойства:
        # _index — индекс помидора (уникальный для каждого объекта)
        # _state — текущая стадия созревания (начинается со стадии отсутствует)
        self._index = index
        self._state = 0

    def grow(self):
        # Переводит томат на следующую стадию созревания, пока не достигнут максимальный этап
        if self._state < max(Tomato.states.keys()):
            self._state += 1

    def is_ripe(self):
        # Проверяет, созрел ли томат (стадия 'красный')
        return self._state == max(Tomato.states.keys())

    def get_state(self):
        # Возвращает текущую стадию созревания текстом
        return Tomato.states[self._state]


class TomatoBush:
    def __init__(self, count):
        # Динамическое свойство tomatoes — список из count помидоров (объектов Tomato)
        self.tomatoes = [Tomato(index) for index in range(1, count + 1)]

    def grow_all(self):
        # Переводит все помидоры в списке на следующую стадию созревания
        for tomato in self.tomatoes:
            tomato.grow()

    def all_are_ripe(self):
        # Проверяет, что все помидоры созрели
        return all(tomato.is_ripe() for tomato in self.tomatoes)

    def give_away_all(self):
        # Очищает список помидоров после сбора урожая
        self.tomatoes.clear()


class Gardener:
    def __init__(self, name, plant):
        # Динамические свойства:
        # name — публичное свойство с именем садовника
        # _plant — приватное свойство с растением (объект класса TomatoBush)
        self.name = name
        self._plant = plant

    def work(self):
        # Садовник ухаживает за растением, вызывая его рост
        print(f'Садовник {self.name} ухаживает за растением...')
        self._plant.grow_all()

    def harvest(self):
        # Пробует собрать урожай, если все помидоры созрели
        if self._plant.all_are_ripe():
            print('Все помидоры созрели! Садовник собирает урожай.')
            self._plant.give_away_all()
        else:
            print('Помидоры еще не созрели. Нужно продолжать ухаживать.')

    @staticmethod
    def knowledge_base():
        # Выводит справку по садоводству
        print('''
        Справка по садоводству:
        1. Садовник ухаживает за растением, заставляя его расти.
        2. Томаты проходят стадии созревания: отсутствует, цветение, зеленый, красный.
        3. Растение можно собирать, только когда все томаты созрели.
        ''')


# Тестирование работы программы

# Создание куста с 3 томатами и садовника с именем Иван
bush = TomatoBush(3)
gardener = Gardener('Иван', bush)



# Сбор урожая, когда томаты созрели
gardener.harvest()

# Проверка, что после сбора урожая томатов на кусте нет
print('Томаты на кусте после сбора:', bush.tomatoes)  # ожидается пустой список
```
### Результат.
![Скриншот задания](pic_9/сам9_5.jpg)
## Выводы

Производится успешный сбор урожая при полной спелости томатов, после чего список томатов очищается, что подтверждается пустым списком в выводе. Все работы демонстрируют ключевые элементы ООП: инкапсуляцию через приватные атрибуты, управление состоянием объектов, наследование и статические методы для справочных функций. Таким образом, поэтапно моделируется реальный жизненный сценарий ухода за растением и сбора урожая с помощью объектно-ориентированного подхода.

## Общие выводы по теме

Последовательно раскрываются основные концепции объектно-ориентированного программирования (ООП) на языке Python. На протяжении работ демонстрируется создание и использование классов и объектов, где классы являются шаблонами для создания экземпляров с определёнными свойствами и поведением. Применяется инкапсуляция — управление доступом к внутренним данным через методы-геттеры и сеттеры, что обеспечивает защиту и простоту обеспечения целостности данных. Реализуется наследование, позволяющее создавать древовидные иерархии классов, где подклассы наследуют свойства и методы родителей, что помогает избегать повторного кода и увеличивает читаемость. Кроме того, показывается полиморфизм, позволяющий использовать один и тот же интерфейс для разных типов объектов, что повышает гибкость и расширяемость программы. Вспомогательные механизмы, такие как статические методы и ограничение атрибутов с помощью slots, дополняют общую картину инструментов ООП в Python. Итоговый набор примеров моделирует реальные задачи, например, процесс созревания и сбора урожая помидоров, что помогает закрепить теоретические знания практическими навыками программирования на основе объектно-ориентированного подхода.





























